// Bootstraps the SPA from the @shared package (see ygo-shared/).
// All app source lives in ygo-shared/src/ and is consumed via the
// '@shared/*' Vite + TypeScript path alias.
import '@shared/main'
