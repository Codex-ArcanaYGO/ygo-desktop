include: { local: .ci/_base.yml }

test:unit:
  extends: .node-base
  stage: test
  script: [npm test]
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
